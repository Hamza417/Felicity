<?xml version="1.0" encoding="utf-8"?>
<app.simple.felicity.decorations.theme.ThemeFrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!--
        The recycler view holds the trial status info cards at the top.
        The bottom padding leaves room for the big buy button below it.
    -->
    <app.simple.felicity.decorations.views.SpacingRecyclerView
        android:id="@+id/recycler_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layoutAnimation="@anim/list_animation_controller"
        android:clipToPadding="false"
        android:paddingBottom="96dp"
        android:paddingTop="8dp"
        android:scrollbars="none"
        app:navigationPaddingRequired="true"
        app:statusPaddingRequired="false" />

    <!--
        The header floats over the recycler view and hides as the user scrolls down,
        so they can see more content without losing navigation context.
    -->
    <app.simple.felicity.decorations.views.AppHeader
        android:id="@+id/header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:headerContentLayout="@layout/header_preferences_generic" />

    <!--
        This is the big, friendly "Buy" button anchored to the bottom of the screen.
        We anchor it to the bottom so users always know where to tap, no scrolling required.
    -->
    <LinearLayout
        android:id="@+id/purchase_button_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:orientation="vertical"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:paddingBottom="16dp"
        app:navigationPaddingRequired="true">

        <app.simple.felicity.decorations.highlight.HighlightTextView
            android:id="@+id/buy_button"
            android:layout_width="match_parent"
            android:layout_height="64dp"
            android:gravity="center"
            android:textSize="@dimen/text_size_18"
            android:textStyle="bold"
            android:text="@string/purchase_now" />

    </LinearLayout>

</app.simple.felicity.decorations.theme.ThemeFrameLayout>

