<?xml version="1.0" encoding="utf-8"?><!--
    1×4 Felicity player widget — a single compact row.

    Structure (left → right):
      [Album Art]  |  Title / Artist  |  ⏮  ▶/⏸  ⏭

    The background ImageView has NO src in XML; the GradientDrawable
    background is rasterized at runtime in FelicityWidgetProvider using
    the active theme color and the user's corner-radius preference.
    This way there are zero hard-coded colors or drawable XML files involved.
-->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!--
        Background layer — no src, no tint, nothing from XML.
        FelicityWidgetProvider draws a GradientDrawable onto a Bitmap at the
        widget's actual pixel size and sets it here via setImageViewBitmap().
        fitXY lets it stretch to fill regardless of launcher-specific sizing.
    -->
    <ImageView
        android:id="@+id/widget_background_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:importantForAccessibility="no"
        android:scaleType="fitXY" />

    <!--
        Single horizontal content row placed on top of the background.
        4dp padding keeps content clear of the rounded corners.
    -->
    <LinearLayout
        android:id="@+id/widget_content"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:padding="4dp">

        <!--
            Square album art — fills the content height.
            Width is a fixed dimen calibrated to the 1-row widget height so it
            stays perfectly square. centerCrop handles non-square source images.
        -->
        <ImageView
            android:id="@+id/widget_album_art"
            android:layout_width="@dimen/widget_album_art_size"
            android:layout_height="match_parent"
            android:contentDescription="@string/album_art"
            android:scaleType="centerCrop" />

        <!--
            Title + artist stacked vertically, filling all the horizontal space
            between the album art and the control buttons.
        -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_marginStart="8dp"
            android:layout_weight="1"
            android:gravity="center_vertical"
            android:orientation="vertical">

            <TextView
                android:id="@+id/widget_title"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:maxLines="1"
                android:text="@string/unknown_title"
                android:textSize="13sp"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/widget_artist"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:maxLines="1"
                android:text="@string/unknown_artist"
                android:textSize="11sp" />

        </LinearLayout>

        <!--
            Previous, play/pause, and next — packed tightly on the right side.
            background="@null" in XML clears the system default ripple highlight;
            we also clear it in code after every RemoteViews push just to be safe.
        -->
        <ImageButton
            android:id="@+id/widget_prev"
            android:layout_width="32dp"
            android:layout_height="match_parent"
            android:background="@null"
            android:contentDescription="@string/previous"
            android:scaleType="fitCenter"
            android:src="@drawable/ic_skip_previous" />

        <ImageButton
            android:id="@+id/widget_play_pause"
            android:layout_width="36dp"
            android:layout_height="match_parent"
            android:background="@null"
            android:contentDescription="@string/play_pause"
            android:scaleType="fitCenter"
            android:src="@drawable/ic_play" />

        <ImageButton
            android:id="@+id/widget_next"
            android:layout_width="32dp"
            android:layout_height="match_parent"
            android:background="@null"
            android:contentDescription="@string/next"
            android:scaleType="fitCenter"
            android:src="@drawable/ic_skip_next" />

    </LinearLayout>

</RelativeLayout>
